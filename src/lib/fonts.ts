import { Inter, Roboto_Mono, Platypi } from "next/font/google";

export const inter = Inter({
    subsets: ["latin"],
    display: "swap",
});

export const roboto_mono = Roboto_Mono({
    subsets: ["latin"],
    display: "swap",
});

export const platypi = Platypi({
    subsets: ["latin"],
    display: "swap",
});
